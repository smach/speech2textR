% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mistral.R
\name{mistral_transcript_to_txt}
\alias{mistral_transcript_to_txt}
\title{Convert Mistral transcript to plain text format}
\usage{
mistral_transcript_to_txt(
  transcript,
  file_path = NULL,
  include_timestamps = FALSE,
  include_speakers = TRUE,
  timestamp_format = "time"
)
}
\arguments{
\item{transcript}{A transcript object returned by mistral_transcribe().}

\item{file_path}{Character string. Path where the text file should be saved.
If NULL, returns the text content as a string without saving.}

\item{include_timestamps}{Logical. If TRUE, includes timestamps for each
speaker section. Default is FALSE.}

\item{include_speakers}{Logical. If TRUE and speaker information is
available, includes speaker labels. Default is TRUE.}

\item{timestamp_format}{Character string. Format for timestamps: "seconds"
(e.g., 123.45s) or "time" (e.g., 00:02:03). Default is "time".}
}
\value{
If file_path is provided, invisibly returns the file path.
  Otherwise returns the text content as a character string.
}
\description{
Converts a Mistral transcript object to plain text with optional
timestamps and speaker labels.
}
\examples{
\dontrun{
transcript <- mistral_transcribe("interview.mp3", diarize = TRUE)

# Save as plain text
mistral_transcript_to_txt(transcript, "transcript.txt")

# With timestamps and speakers
mistral_transcript_to_txt(
  transcript,
  "transcript_detailed.txt",
  include_timestamps = TRUE,
  include_speakers = TRUE
)

# Get as string
text <- mistral_transcript_to_txt(transcript)
}

}
